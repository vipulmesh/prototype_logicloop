'use client';import {useState} from 'react';import {AppShell} from '@/components/shell';import {Card,Stat,Badge} from '@/components/ui';import {candidates,chartData} from '@/lib/data';import {Search,Sparkles,Users,UserCheck,Clock,Briefcase} from 'lucide-react';import {LineChart,Line,ResponsiveContainer,XAxis,Tooltip,PieChart,Pie,Cell} from 'recharts';
export default function Recruiter(){const [q,setQ]=useState('Find Java developers with hackathon experience');const results=candidates.filter(c=>!q||`${c.role} ${c.skills.join(' ')}`.toLowerCase().includes(q.toLowerCase().split(' ')[1]||'' )).slice(0,6);return <AppShell><p className="label">Recruitment command center</p><h1 className="mt-1 text-3xl font-bold">Find exceptional people</h1><Card className="mt-7"><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-500" size={18}/><input className="input pl-10" value={q} onChange={e=>setQ(e.target.value)} placeholder="Describe the talent you need..."/></div><button className="btn btn-primary"><Sparkles size={16}/>AI shortlist</button></div><p className="mt-3 text-xs text-slate-500">Try: “Senior React engineers with fintech product experience”</p></Card><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Active candidates" value="2,418" trend="12% this month" icon={<Users/>}/><Stat label="Shortlisted" value="184" trend="24 today" icon={<UserCheck/>}/><Stat label="Time to hire" value="21d" trend="6 days faster" icon={<Clock/>}/><Stat label="Open roles" value="25" trend="4 new roles" icon={<Briefcase/>}/></div><div className="mt-5 grid gap-5 xl:grid-cols-3"><Card className="xl:col-span-2"><div className="flex justify-between"><h2 className="font-semibold">AI-ranked talent</h2><Badge>Top 6 matches</Badge></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{results.map(c=><div key={c.id} className="rounded-xl border border-line bg-white/[.03] p-4"><div className="flex items-center justify-between"><span className="grid h-9 w-9 place-items-center rounded-full bg-cyan-400/15 text-xs text-cyan-200">{c.avatar}</span><Badge color="green">{c.score}% fit</Badge></div><h3 className="mt-4 font-medium">{c.name}</h3><p className="mt-1 text-xs text-slate-400">{c.role} · {c.experience} yrs</p><div className="mt-3 flex flex-wrap gap-1">{c.skills.slice(0,2).map(s=><Badge key={s}>{s}</Badge>)}</div><button className="btn btn-ghost mt-4 w-full py-2 text-xs">Review profile</button></div>)}</div></Card><Card><h2 className="font-semibold">Hiring funnel</h2>{[['Sourced',842],['Screening',316],['Interview',94],['Offer',27]].map(x=><div key={x[0]} className="mt-4"><div className="flex justify-between text-sm"><span>{x[0]}</span><span>{x[1]}</span></div><div className="mt-2 h-2 rounded bg-slate-800"><div className="h-2 rounded bg-gradient-to-r from-cyan-500 to-violet-500" style={{width:`${x[1]/8.42}%`}}/></div></div>)}</Card></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card><h2 className="font-semibold">Recruitment velocity</h2><div className="mt-4 h-52"><ResponsiveContainer width="100%" height="100%"><LineChart data={chartData}><XAxis dataKey="m" stroke="#64748b"/><Tooltip/><Line dataKey="hires" stroke="#a78bfa" strokeWidth={3}/></LineChart></ResponsiveContainer></div></Card><Card><h2 className="font-semibold">Pipeline distribution</h2><div className="mt-2 h-52"><ResponsiveContainer><PieChart><Pie data={[{value:55},{value:25},{value:14},{value:6}]} dataKey="value" innerRadius={50} outerRadius={80}>{['#22d3ee','#8b5cf6','#34d399','#fbbf24'].map(c=><Cell key={c} fill={c}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></Card></div></AppShell>}
