'use client';
import {motion} from 'framer-motion'; import {ArrowUpRight, CheckCircle2, Sparkles} from 'lucide-react'; import {ReactNode} from 'react';
export function Card({children,className='' }:{children:ReactNode,className?:string}){return <motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className={`glass rounded-2xl p-5 ${className}`}>{children}</motion.div>}
export function Badge({children,color='cyan'}:{children:ReactNode,color?:'cyan'|'violet'|'green'|'amber'}){const c={cyan:'bg-cyan-400/10 text-cyan-300 border-cyan-400/20',violet:'bg-violet-400/10 text-violet-300 border-violet-400/20',green:'bg-emerald-400/10 text-emerald-300 border-emerald-400/20',amber:'bg-amber-400/10 text-amber-300 border-amber-400/20'}[color];return <span className={`inline-flex rounded-full border px-2.5 py-1 text-xs ${c}`}>{children}</span>}
export function Stat({label,value,trend,icon}:{label:string,value:string|number,trend?:string,icon?:ReactNode}){return <Card><div className="flex justify-between"><span className="label">{label}</span><span className="text-cyan-300">{icon}</span></div><div className="mt-3 text-3xl font-bold">{value}</div>{trend&&<div className="mt-2 flex items-center gap-1 text-xs text-emerald-400"><ArrowUpRight size={13}/>{trend}</div>}</Card>}
export function Progress({label,value}:{label:string,value:number}){return <div className="mb-4"><div className="mb-2 flex justify-between text-sm"><span className="text-slate-300">{label}</span><span className="font-semibold">{value}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-800"><motion.div initial={{width:0}} animate={{width:`${value}%`}} className="h-full rounded-full bg-gradient-to-r from-cyan-500 to-violet-500"/></div></div>}
export function EmptyAction({title,action}:{title:string,action:string}){return <div className="py-8 text-center text-slate-400"><Sparkles className="mx-auto mb-3 text-cyan-300"/><p>{title}</p><button className="btn btn-primary mt-4 text-sm">{action}<CheckCircle2 size={15}/></button></div>}
